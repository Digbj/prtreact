import { useState } from "react"

const Bookmark=(prop)=>{
    // const [book,setbook]=useState([])
    // setbook(prop)
    return(<div>
{/* {book.map((data,index)=>{
    return(
        <div>
            <img src={data} alt="pic"/>
            </div>

    )
})} */}
<p>bookmark</p>
    </div>)
}
export default Bookmark;